import { notFound } from "next/navigation"; import { PageShell } from "@/components/site/page-shell"; import { MiniPreviews } from "@/components/product/mini-previews"; import { Reveal } from "@/components/site/reveal"; import { isLocale } from "@/lib/content"; import { AudioLines, Route, MessagesSquare } from "lucide-react";
const capabilities=[[AudioLines,"AI speech coach","Actionable feedback on clarity, pace, and pronunciation—not a vague score."],[Route,"Adaptive pathways","Lessons respond to your progress instead of forcing one fixed course."],[MessagesSquare,"Purposeful practice","Prepare for interviews, classrooms, work, and everyday conversation."]] as const;
export default async function Product({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const od=locale==="or";return <PageShell locale={locale} page="product" eyebrow="THE E-TALK EXPERIENCE" title={od?"ବୁଝନ୍ତୁ। କୁହନ୍ତୁ। ପ୍ରତିଦିନ ବଢ଼ନ୍ତୁ।":"Understand it. Speak it. Grow every day."} intro={od?"ଓଡ଼ିଆର ସ୍ପଷ୍ଟତାରୁ ଆରମ୍ଭ କରି ବାସ୍ତବ ଇଂରାଜୀ କଥାବାର୍ତ୍ତା ପର୍ଯ୍ୟନ୍ତ ଏକ ବ୍ୟକ୍ତିଗତ ଯାତ୍ରା।":"A personal journey from Odia-supported clarity to English you can use in real conversations."}><section className="section product-gallery"><div className="shell"><Reveal><div className="gallery-heading"><span className="eyebrow">ONE LEARNING LOOP</span><p>Three connected views. One clear next step every day.</p></div></Reveal><MiniPreviews locale={locale}/></div></section><section className="section capability-section"><div className="shell capability-list">{capabilities.map(([Icon,title,body],index)=><Reveal key={title}><article className="capability-row"><span>0{index+1}</span><div className="capability-icon"><Icon strokeWidth={1.45}/></div><h2>{title}</h2><p>{body}</p></article></Reveal>)}</div></section></PageShell>}
