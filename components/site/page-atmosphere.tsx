"use client";
import { motion, useMotionValue, useSpring } from "motion/react";
export function PageAtmosphere(){const x=useMotionValue(75),y=useMotionValue(25);const sx=useSpring(x,{stiffness:55,damping:22}),sy=useSpring(y,{stiffness:55,damping:22});return <div className="page-atmosphere" onPointerMove={event=>{const box=event.currentTarget.getBoundingClientRect();x.set(((event.clientX-box.left)/box.width)*100);y.set(((event.clientY-box.top)/box.height)*100)}} aria-hidden><motion.div className="page-spotlight" style={{left:sx,top:sy}}/><div className="page-grid-lines"/><motion.i className="page-comet comet-one" animate={{x:[-120,900],y:[80,340],opacity:[0,1,0]}} transition={{duration:8,repeat:Infinity,repeatDelay:4}}/><motion.i className="page-comet comet-two" animate={{x:[600,-400],y:[30,260],opacity:[0,.7,0]}} transition={{duration:10,repeat:Infinity,repeatDelay:6}}/></div>}
